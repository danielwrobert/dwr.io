---
title: 'Test'
date: ''
slug: test
excerpt: ''
draft: true
---

Here's some JS code to test...

```javascript
function someFunction() {
    return {
        dog: 'woof',
        cat: 'meow',
        bird: 'chirp',
    }['bird'];
}
```

Another block of JS:

```javascript
const currentFilter = 'all';
 
function someFunction(items) {
    return {
        all: items,
        completed: items.filter( item => item.completed ),
        incomplete: items.filter( item => item.incomplete ),
    }[currentFilter];
}
```

Try some CSS:

```css
.container {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  grid-gap: 20px;
}
@supports not (display: grid) {
  .container {
    display: flex;
    flex-wrap: wrap;
  }
}
```

And PHP:

```php
if ( true == $the_force ) {
  $victorious = you_will( $be );
}
```
