---
title: "Understanding JavaScript's Event Loop"
date: ''
slug: understanding-javascripts-event-loop
excerpt: "A look at JavaScript's single-threaded runtime, the event queue, task queue, and microtasks."
category: 'JavaScript'
tags: ['event-loop']
draft: true
---

**NOTE: This post can be replaced by a more recent draft in my post list. Once that post is published, delete this draft.**

The main JavaScript runtime is single threaded. Two functions can’t run at the same time. The runtime contains an Event Queue which stores a list of messages to be processed. There are no deadlocks, so there is no need to use locks. However, the code in the Event Queue needs to run fast. Otherwise the browser will become unresponsive and will ask to kill the task.

## The Task Queue

## Microtasks
